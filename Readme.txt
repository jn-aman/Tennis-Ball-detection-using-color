> This python script detect Tennis ball based on color filtering technique
> It can only detect yellow or green colored ball.
> Make sure you don't have green color ball in background
> You can change HSV color values to detect other color objects